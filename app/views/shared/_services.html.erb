<div class="services-list">
  <%= render 'shared/services_header' %>

  <%# Header for the services %>
  <div class="services-header">
    <p class="fs-6"><%= "#{services.count} #{'service'.pluralize(services.count)}" %> available</p>
  </div>

  <%# Grid for services cards %>
  <div class="row">
    <% services.each do |service| %>
      <div class="col-3 mb-4">
        <%= render 'users/service_card', service: service, user: service.seller %>
      </div>
    <% end %>
  </div>
</div>
